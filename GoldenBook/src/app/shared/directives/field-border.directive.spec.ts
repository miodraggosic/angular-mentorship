import { FieldBorderDirective } from './field-border.directive';

describe('FieldBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldBorderDirective();
    expect(directive).toBeTruthy();
  });
});
